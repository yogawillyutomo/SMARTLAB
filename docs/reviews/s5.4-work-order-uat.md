# S5.4 Work Order Storage-Cleared UAT

**Status:** PRE-MERGE EVIDENCE COMPLETE; WORK ORDER LIFECYCLE + GLOBAL LAB CONTEXT BROWSER UAT PASS; STACKED MERGE AUTHORIZATION PENDING  
**S5.3 merged base:** `main@5835b10a116c0e9fba0319ce697cfd608824052a`  
**Current stacked candidate:** PR #88 `c9947de27ffebd99d1c4f634086ff18d1163771f` → PR #89 runtime `1c4aca20c289526dc912322c7c3fd87413e3869c`  
**Automated proof:** original PR #88 workflow #335 passed web/API/Vercel; exact runtime head `1c4aca20...` has Vercel SUCCESS, operator web parity PASS, Global Lab Context fixture tests 3/3, Activity Report map serialization 1/1, LaboratorySessionApiTest 17/17, and full API 735 PASS / 11 SKIP / 0 FAIL / 5894 assertions. No GitHub Actions run exists for this exact head  
**Purpose:** prove that Corrective Work Order UI is server-authoritative and preserves the S5.1–S5.3 authority locks before S5 closure.

This file separates automated evidence, manual functional lifecycle evidence, and final impacted browser revalidation. The Work Order lifecycle itself has been exercised successfully, but do not mark S5 complete until storage-cleared/bootstrap evidence, Global Laboratory Context impact checks, stacked PR merge order, and post-merge regression are complete.

## 1. Automated evidence

At implementation SHA `02bb3be82f162780a92cff7175c7f62f8b7881da`:

- `/work-orders` and `/work-orders/:id` use `RequireServerPermission permission="work-orders.view"`;
- navigation uses server `work-orders.view`;
- `WorkOrdersPage` has no `useAppData`, `db.workOrders`, local stock mutation, local Device mutation, or mutable cost/finance authority;
- the typed `workOrderApi` gateway validates canonical response fields and strong `If-Match`;
- exact Asset + Laboratory are selected from canonical APIs;
- optional Incident linkage uses canonical Incident IDs and filters Device-linked Incidents against the selected exact Asset;
- assignment candidates are derived from active server memberships whose server role permissions include `work-orders.update`;
- part use calls `POST /work-orders/{id}/parts` and Inventory remains quantity authority;
- completion records evidence only;
- verification calls `POST /work-orders/{id}/verify`; Asset mutation remains server-side Asset authority;
- no browser action sets Device Online, closes Incident, blocks a whole Laboratory, or writes finance state;
- `SourceOfTruthBoundary.test.ts` locks the production route against browser-local Work Order authority;
- `workOrderApi.test.ts` locks canonical lifecycle, part, history, verify, and ETag behavior.

The first full cutover head `b928814d74abe41daa0c1eca00791cd1ba563338` passed workflow #333 with 65 web test files / 837 tests. The later assignment and optional-Incident refinements also passed exact-head CI; workflow #335 is the current implementation proof.

## 2. Local preparation

Use the exact PR branch and keep history intact:

```powershell
cd D:\KODE\SmartLab\SMARTLAB
git fetch origin
git switch feat/s5-work-order-frontend-cutover
git pull --ff-only
git status
git rev-parse HEAD
```

Expected runtime implementation before this evidence-doc commit:

```text
02bb3be82f162780a92cff7175c7f62f8b7881da
```

If the branch has advanced only by evidence/documentation commits, record the exact tested SHA in section 5. If runtime files changed, re-run impacted browser checks.

API preparation:

```powershell
cd D:\KODE\SmartLab\SMARTLAB\apps\api
php artisan migrate:status
php artisan db:seed --class=PermissionSeeder
php artisan db:seed --class=RolePermissionSeeder
php artisan serve --host=127.0.0.1 --port=8000
```

Do not use `migrate:fresh`, `migrate:reset`, or `migrate:refresh`.

Web:

```powershell
cd D:\KODE\SmartLab\SMARTLAB\apps\web
npm run dev
```

If dependencies are not installed, use `npm ci` first.

## 3. Storage-cleared bootstrap

In Microsoft Edge / Chromium:

1. open DevTools → Application;
2. clear Local Storage, Session Storage, IndexedDB, Cache Storage, cookies, and Service Worker state for the SmartLab origin;
3. hard reload;
4. authenticate again;
5. open `/work-orders`.

Acceptance:

- page loads from server APIs;
- legacy local/seed Work Orders do **not** appear merely because browser AppData seed storage exists;
- refresh does not change canonical count/status;
- direct deep link `/work-orders/{canonicalUlid}` resolves the same server record;
- no browser-local Work Order is imported automatically.

## 4. Manual functional matrix

Use safe UAT records only.

### A. Canonical create and persistence

Prepare or select:

- one active canonical Asset;
- one active Laboratory;
- optionally one non-closed/non-rejected Incident;
- if the Incident references a Device, the selected Asset must be exact-linked to that Device.

Create a Work Order with a recognizable problem summary.

Expected:

- server-generated `WO-YYYY-NNNNNN`;
- status `Draft`;
- exact Asset/Lab snapshots visible;
- optional Incident linkage persists when supplied;
- hard reload retains the Work Order;
- no local cost field is created.

### B. Assignment and corrective custody

Assign an eligible server membership.

Expected:

- technician comes from server membership evidence, not a hardcoded display-name list;
- status becomes `Ditugaskan`;
- start moves it to `Berlangsung`;
- corrective custody becomes active;
- Asset operational state becomes `in_repair`;
- Loan checkout and Preventive Maintenance start for that exact Asset remain fail-closed by server authority.

### C. Hold / waiting part / resume

Exercise:

`in_progress -> on_hold -> waiting_part -> in_progress`.

Expected:

- reasons persist in Work Order history;
- corrective custody remains active throughout;
- `waiting_part` by itself does not decrement or reserve stock.

### D. Inventory-authoritative spare part

Before the test, create/use a canonical InventoryItem with known balance, for example 5.

While Work Order is `in_progress`:

- issue quantity 1 through **Gunakan Spare Part**;
- refresh the Work Order;
- open `/stock`.

Expected:

- Work Order history shows part evidence;
- Inventory balance changes 5 → 4 through immutable sourced `issue`;
- no browser-created stock transaction exists;
- retrying the same mutation ID does not decrement stock again;
- Work Order UI shows no fabricated finance/cost total.

### E. Completion boundary

Complete with:

- diagnosis;
- action taken;
- condition after;
- optional test result.

Expected before verification:

- Work Order status = `Selesai Teknis`;
- corrective custody remains active;
- Asset condition is still the pre-verification canonical condition;
- Device lifecycle/operational status is not mutated;
- Incident status is not changed automatically.

### F. Managerial verification

Using a membership with `work-orders.approve`, click **Verifikasi**.

Expected:

- Work Order status = `Terverifikasi`;
- corrective custody releases;
- proposed condition is applied through Asset authority;
- Asset audit includes `asset.work_order_condition_updated`;
- operational state no longer reports `in_repair` unless another blocker exists;
- no Device state change;
- no Incident auto-resolution/verification/closure;
- no Laboratory-wide closure/blocker is created.

### G. Refresh / deep-link persistence

After create, assignment, part issue, completion, and verification:

- hard reload `/work-orders`;
- deep-link to the Work Order detail;
- navigate away and back.

Expected:

- number, snapshots, status, technician, history, part evidence, completion/verification evidence persist;
- browser storage clear followed by login still reconstructs the same canonical state from server APIs.

### H. Permission sanity

At minimum, verify with available roles:

- a viewer without create cannot see **Work Order Baru**;
- a technician with `work-orders.update` can progress eligible assigned work but cannot verify without approve;
- consume-stock action is absent without `work-orders.consume-stock`;
- verification is absent without `work-orders.approve`;
- route access itself requires `work-orders.view`.

Do not claim cross-tenant browser PASS unless a separate School/account scenario is actually executed. Exact tenant/RBAC isolation remains covered by automated API tests.

## 5. Manual evidence record

Evidence actually executed during the S5 UAT sequence is recorded below. Items not explicitly exercised remain pending rather than inferred.

| Field | Evidence |
| --- | --- |
| Date | 2026-09-09 functional lifecycle; 2026-09-10 exact-head web parity |
| Operator | Project operator / Admin UAT SmartLab / Teknisi UAT |
| Browser/version | Chromium-family browser used for screenshots; exact browser version not separately recorded |
| Exact browser-tested SHA | lifecycle and authority separation proven through `0a07ae1...`; assignment presentation revalidated after `34019bba...`; later Dashboard/Monitoring visual UAT confirmed by operator |
| Storage cleared + fresh login | **PENDING — not separately evidenced** |
| Canonical create + reload | PASS — `WO-2026-000001` persisted canonically; `WO-2026-000002` later created for Dashboard/Lab-context UAT |
| Optional Incident compatibility | no linked Incident used in the main lifecycle; automated contract remains the evidence |
| Assignment + start/custody | PASS — Technician UAT assignment, start, and corrective custody proven |
| Hold/wait/resume | PASS — waiting-part/resume history persisted; custody remained active |
| Part issue + stock ledger | PASS — one exact `UAT-RAM-16GB` issue, stock 5→4, WorkOrderPartUsage count 1, immutable sourced InventoryTransaction |
| Completion boundary | PASS — Work Order completed at version 7 while Asset stayed good/version 2, Device stayed RAM 8/version 2 until explicit Device authority edit, stock stayed 4, Incident count stayed 0 |
| Explicit Device authority | PASS — canonical Device UI changed PC001 RAM 8→16, Device version 2→3, `device.technical_profile_replaced`; Work Order/Asset/Inventory/Incident remained unchanged |
| Verification + Asset audit/custody release | PASS — Work Order verified version 8, custody false, Asset event `asset.work_order_condition_updated`, Asset stayed good/version 2 |
| No Device implicit mutation | PASS |
| No Incident implicit lifecycle mutation | PASS — Incident count remained 0 |
| Deep-link persistence | PASS for exercised Work Order detail/history reloads; final Global Lab Context impact revalidation still pending |
| Permission sanity | PASS for Technician vs Admin verification boundary and canonical assignment candidate; exhaustive viewer matrix not separately browser-executed |
| Exact-head web parity | PASS on unchanged frontend lineage: typecheck; SourceOfTruthBoundary 23/23; production build |
| Full API regression | PASS at `1c4aca20...`: 735 PASS / 11 SKIP / 0 FAIL / 5894 assertions |
| Timezone regression | PASS — PostgreSQL connection pinned to UTC; runtime `SHOW TIME ZONE = UTC`; `WO-2026-000003` Draft→Cancelled immediately displayed `baru saja` and persisted `2026-09-10 00:31:05+00` consistently |
| Global Lab data-bearing UAT | PASS — dedicated non-custody fixtures cover Incident, Schedule, Reservation, Session/ActivityReport, and Operational Calendar; operator confirmed all other pages safe and Pelaksanaan Lab RPL1/RPL2 isolation after the Activity Report serialization fix |
| Activity Report contract regression | PASS — empty `commonContent` / `typeSpecificContent` serialize as JSON objects; targeted 1/1 and LaboratorySessionApiTest 17/17 PASS |
| Screenshots/log references | operator screenshots plus read-only Laravel Tinker and full test outputs recorded in PR #89 conversation/body |
| Overall manual browser UAT | **PASS — PRE-MERGE EVIDENCE COMPLETE** |

## 6. S5 closure gate

S5 may be marked complete only when all are true:

```text
S5_1_CONTRACT_LOCKED = TRUE
S5_2_CORE_MERGED = TRUE
S5_3_INVENTORY_VERIFY_MERGED = TRUE
S5_4_AUTOMATED_CUTOVER_CI = PASS
S5_4_MANUAL_BROWSER_UAT = PASS
PR_88_MERGED = TRUE
POST_MERGE_WEB_CI = PASS
POST_MERGE_API_CI = PASS
S5_COMPLETE = TRUE
```

The canonical status is **S5 at merge gate / not yet closed**. Work Order lifecycle, authority separation, timezone runtime behavior, web parity, full API regression, and data-bearing Global Laboratory Context browser UAT are proven. No pre-merge evidence gap remains. S5 still requires explicit merge authorization, verified stacked merge order, and post-merge regression before closure.
