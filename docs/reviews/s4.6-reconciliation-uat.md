# S4.6 Reconciliation & Storage-Cleared UAT

**Status:** browser UAT evidence recorded; post-evidence exact-head CI pending  
**S4.6 base:** merged S4.5 `main@e3da257c52f56de33fd61d233d2a30bb14c8348f`  
**Current implementation/browser-tested SHA before this evidence commit:** `2572d5c2511a69ad36b8473253ecc96a2c58363d`  
**Purpose:** prove S4 cross-domain invariants and source-of-truth closure without adding S5 Corrective Work Order authority.

This document is an evidence matrix, not a substitute for repository state or CI. GitHub branch/PR/commit state, migrations, tests, OpenAPI, and exact-head CI remain authoritative.

Related contracts:

- [S4 Asset / Inventory / Loan / Preventive Maintenance Contract](../architecture/asset-inventory-loan-maintenance-contract.md)
- [Source-of-Truth Migration](../architecture/source-of-truth-migration.md)
- [Root OpenAPI Contract](../../packages/contracts/openapi.yaml)

## 1. Automated reconciliation matrix

| Proof | Repository mechanism | Acceptance |
| --- | --- | --- |
| Derived Asset operational state | read-only `GET /api/v1/assets/{assetId}/operational-state` | state is derived, has provenance, and never becomes writable Asset authority |
| Lifecycle vs custody | Asset mutation service revalidates active Loan/Maintenance custody for retire/dispose/device-unlink | active custody returns `ASSET_ACTIVE_CUSTODY_CONFLICT` and leaves Asset/link unchanged |
| Negative stock contention | PostgreSQL-only two-worker test: balance 5, issue 4 vs issue 4 | at most one succeeds; final balance is 1; ledger reconstructs balance; never negative |
| Double Loan contention | PostgreSQL-only two-worker checkout against one Asset | at most one succeeds; one active Loan custody remains |
| Loan vs Maintenance contention | PostgreSQL-only simultaneous Loan checkout vs Maintenance start | exactly one custody wins; dual custody never commits |
| Historical evidence | S4 reconciliation API test | later Asset/Inventory metadata changes do not rewrite LoanItem, MaintenanceExecution, or InventoryTransaction snapshots |
| Inventory reconstruction | immutable `signed_delta` evidence compared with canonical balance | ledger sum equals current on-hand balance |
| Asset↔Device boundary | existing one-to-one DB/API tests plus S4 custody tests | exact optional 1:1 link remains; Loan/Maintenance do not take Device lifecycle/home-Laboratory authority |
| S4 browser source-of-truth | aggregate Vitest scan over Assets/Stock/Loans/Maintenance pages | no AppData/browser repository/local business mutation/compatibility permission authority on canonical routes |
| Documentation links | repository Node validation script in CI | relative Markdown links resolve inside the repository |
| OpenAPI | OpenAPI syntax CI + root contract 0.30 | Asset operational-state and versioned in-progress Maintenance checklist progress are machine-described without creating writable availability or S5 authority |

The last automated implementation SHA validated before this evidence commit was `2572d5c2511a69ad36b8473253ecc96a2c58363d`:

- `web-ci`: 64 test files / 833 tests passed;
- `api-ci`: PostgreSQL contention proof passed; portable suite 700 passed, 3 skipped, 5438 assertions;
- `MaintenanceApiTest`, `S4ReconciliationApiTest`, and `UatAdminSeederTest`: PASS;
- Vercel preview: PASS.

After this documentation commit, CI must be reverified on the new exact branch HEAD before PR readiness is changed.

## 2. Migration/import policy — classification only

No automatic browser-data migration is approved by S4.6.

A future explicitly approved import/reconciliation tool must classify every candidate record into exactly one of these outcomes:

| Classification | Meaning | Allowed action |
| --- | --- | --- |
| `exact_safe_match` | one canonical same-School target is proven by stable evidence and all required dependencies agree | may be proposed for explicit import/apply; never infer identity from the browser ID |
| `unmatched` | no canonical target can be proven | preserve as unresolved evidence; do not fabricate a target |
| `ambiguous` | more than one plausible canonical target exists | block automatic apply; require human reconciliation |
| `invalid` | payload/value/scope violates the canonical contract | reject and report the reason |
| `blocked_by_dependency` | the record could be valid but a required canonical dependency is missing or unresolved | defer until dependency reconciliation succeeds |

Hard rules:

- browser/local IDs never become server ULIDs by inference;
- display name alone is never sufficient identity;
- cross-School matching is prohibited;
- Stock quantity becomes an explicit opening/receipt/adjustment transaction, never a hidden balance write;
- historical Loan/Maintenance text that cannot map to an exact Asset remains historical/unresolved evidence;
- S4 import cannot create Work Orders or Incidents implicitly.

## 3. Browser UAT evidence

The manual browser evidence has two intentionally separate layers:

1. a full storage-cleared core workflow run on an earlier PR runtime;
2. an impacted-flow retest on exact implementation SHA `2572d5c2511a69ad36b8473253ecc96a2c58363d` after the checklist-progress and UI-polish commits.

This distinction is required because browser evidence must not be silently carried across impacted code changes.

### 3.1 Earlier full storage-cleared core run

**Result: PASS for the core S4 workflow on the earlier runtime.**

Recorded earlier candidate runtime: approximately `5829fa55ebaa6bee2abbf44b4fa9b962c526a3dd`.

Reset/bootstrap evidence:

- Cache Storage cleared;
- IndexedDB cleared;
- Local Storage and Session Storage cleared;
- cookies cleared;
- Service Worker state cleared;
- hard reload performed;
- fresh authentication succeeded;
- `/assets`, `/stock`, `/loans`, and `/maintenance` loaded after clearing browser business storage;
- historical browser/local prototype data did not reappear.

Core mutation evidence from that run:

| Domain | Evidence | Result |
| --- | --- | --- |
| Asset | created `UAT-S46-ASSET-001`; survived reload/deep-link | PASS |
| Inventory | created `UAT-S46-STOCK-001`; opening +5 persisted in immutable ledger | PASS |
| Loan | submitted → approved → checked_out → returned → closed; custody 1 → 0 on return | PASS |
| Preventive Maintenance | exact-Asset plan/execution started and completed; custody 1 → 0; condition evidence persisted | PASS |
| Inventory from Maintenance | one explicit spare-part issue reduced 5 → 4 through immutable InventoryTransaction | PASS |
| S5 boundary | no implicit Work Order or Incident observed | PASS |

This earlier full run remains historical browser evidence for the unaffected core paths. It is not claimed as exact-head proof for UI/checklist code changed afterward.

### 3.2 Current-head impacted browser retest

**Exact tested implementation SHA:** `2572d5c2511a69ad36b8473253ecc96a2c58363d`  
**Date:** 2026-09-07  
**Environment:** local Windows development environment; PostgreSQL 18.3 at `127.0.0.1:5432`, database `smartlab_dev`; Laravel 13.26.1 / PHP 8.5.9; local Vite frontend with local Laravel API  
**Operator:** Willy, local repository/UAT operator  
**Browser:** Microsoft Edge 152 / Chromium  
**Screenshots:** no screenshot evidence was claimed or committed for this retest.

Local repository and database preconditions were verified before the browser retest:

- local branch: `feat/s4-reconciliation-uat`;
- local HEAD exactly matched remote `2572d5c2511a69ad36b8473253ecc96a2c58363d`;
- `origin/main` remained `e3da257c52f56de33fd61d233d2a30bb14c8348f`;
- working tree was clean;
- forward migration `2026_09_07_000001_add_maintenance_checklist_progress` was already `[3] Ran`;
- no `migrate:fresh`, reset, refresh, rebase, force-push, or history rewrite was used.

#### A. Loan Return modal UI smoke

**Result: PASS.**

On an exact-head Loan in `checked_out` state:

- updated Return modal rendered without the previously observed layout imbalance;
- Asset evidence, return condition control, return notes, helper text, and Incident boundary messaging were usable;
- return mutation succeeded;
- Loan moved to `returned`;
- active Loan custody released from 1 to 0;
- hard reload retained return evidence;
- no Incident was created implicitly;
- return evidence did not directly mutate Asset condition authority.

#### B. Maintenance Plan checklist-template UI

**Result: PASS.**

The exact-head Maintenance Plan UI was exercised with multiple checklist items:

- checklist input/add interaction was usable;
- `Tambah Checklist` worked;
- multiple checklist items were retained;
- numbering/item count and visual hierarchy were usable;
- save and hard reload retained canonical plan checklist data.

#### C. In-progress checklist persistence

**Result: PASS.**

A safe exact-Asset MaintenanceExecution was scheduled and started:

- execution entered `in_progress`;
- Maintenance custody became active;
- only checklist item #1 was checked and saved through the server-authoritative progress action;
- save did not complete the execution;
- custody remained active;
- hard reload retained item #1 checked while item #2 remained unchecked;
- item #2 was then checked and progress saved again;
- a second hard reload retained both checked items;
- opening `Selesaikan` prefilled completion checklist state from saved working progress;
- completion succeeded;
- final `checklistResults` became completion evidence;
- Maintenance custody released;
- no spare-part issue was added during this retest, so Inventory remained unchanged by the progress path;
- no Incident or Work Order was created implicitly.

The working-progress path therefore remained distinct from final completion evidence and did not acquire Asset, Inventory, Incident, Work Order, or Laboratory-wide availability authority.

#### D. Stale version / concurrency evidence

No manual browser concurrency claim is made.

Fail-closed stale-version and PostgreSQL contention evidence remains covered by exact-head automated tests. This document does not fabricate simultaneous-browser evidence.

### 3.3 Tenant-scope note

The current impacted retest did not add a new alternate-account cross-tenant browser scenario. Tenant isolation, active SchoolMembership, SMARTLAB-local permission boundaries, and contention rules remain enforced by the automated exact-head suite and the existing authenticated UAT setup. No cross-tenant browser PASS beyond previously established evidence is claimed here.

## 4. Operator evidence record

| Field | Evidence |
| --- | --- |
| Date | 2026-09-07 |
| Environment | Local Windows development environment; PostgreSQL 18.3 / `smartlab_dev`; Laravel 13.26.1 / PHP 8.5.9; local Vite frontend + Laravel API |
| Exact current-head browser-tested SHA | `2572d5c2511a69ad36b8473253ecc96a2c58363d` |
| Earlier full storage-cleared runtime | approximately `5829fa55ebaa6bee2abbf44b4fa9b962c526a3dd` |
| Operator | Willy |
| Browser/version | Microsoft Edge 152 / Chromium |
| `/assets` core storage-cleared evidence | PASS on earlier full run |
| `/stock` core storage-cleared evidence | PASS on earlier full run |
| `/loans` core evidence | PASS earlier; current-head Return modal impacted retest PASS |
| `/maintenance` core evidence | PASS earlier; current-head checklist-template + checklist-progress impacted retest PASS |
| Cross-tenant browser note | no new alternate-account scenario claimed in impacted retest; automated tenant boundary evidence remains authoritative |
| Screenshots/log references | none claimed for the current impacted retest |
| Overall required browser evidence for current S4.6 candidate | **PASS**, composed of earlier full storage-cleared core evidence plus exact-head impacted-flow retest |

## 5. S4 closure rule

Browser evidence is now recorded, but S4 is not complete merely because browser UAT passed.

S4 may be called complete only after all of the following are true:

1. post-evidence exact-head automated CI is green, including the PostgreSQL contention suite;
2. source-of-truth and relative-doc-link gates are green;
3. browser evidence remains bound to the implementation SHA(s) recorded above;
4. PR #83 is merged only after explicit merge authorization;
5. the exact merged `main` head is reverified green;
6. documentation is reconciled to the merged state without claiming production deployment readiness.

Immediately after this evidence commit and before its CI finishes, the correct status is:

```text
S4.6_BROWSER_EVIDENCE = PASS
POST_EVIDENCE_EXACT_HEAD_CI = PENDING
S4_COMPLETE = FALSE
```

Maintenance Campaign / Batch orchestration remains future work. It may coordinate many exact-Asset MaintenanceExecutions for one Laboratory, but it must not turn one MaintenanceExecution into ambiguous multi-Asset authority or implicitly block the whole Laboratory.
